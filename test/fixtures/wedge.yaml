name: unprovided-input
title: unprovided input deadlock
description: minimal strict-input deadlock witness

inputs:
  - name: contract
    seedOwed: true

steps:
  - name: work
    consumes: [contract]
    produces: [outcome]
    terminal: true
