{
  "version": 1,
  "catalog": [
    {
      "name": "code-delivery",
      "title": "Code delivery",
      "description": "Plan, implement, test, and review an approved change in an existing software repository.",
      "inputs": ["proposal"],
      "inputSchemas": [
        {
          "name": "proposal",
          "schema": {
            "type": "object",
            "required": ["text"],
            "properties": {
              "text": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        }
      ],
      "x": {
        "discovery": {
          "description": "Plan, implement, test, and review an approved change in an existing software repository.",
          "whenToUse": ["Deliver an approved change in an existing software repository."],
          "notFor": ["Research and curate a topical media or knowledge library."],
          "interface": {
            "inputs": [
              {
                "name": "proposal",
                "summary": "The approved repository change to deliver.",
                "schemaRef": "#/inputs/0/schema"
              }
            ],
            "outputs": []
          }
        }
      }
    },
    {
      "name": "library-build",
      "title": "Library build",
      "description": "Research a topic across subtopics, acquire source and media artifacts, and synthesize a linked knowledge library.",
      "inputs": ["topic"],
      "inputSchemas": [
        {
          "name": "topic",
          "schema": {
            "type": "object",
            "required": ["text"],
            "properties": {
              "text": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        }
      ],
      "x": {
        "discovery": {
          "description": "Research a topic across subtopics, acquire source and media artifacts, and synthesize a linked knowledge library.",
          "whenToUse": ["Build a sourced media and knowledge library on a defined topic."],
          "notFor": ["Implement and review an approved software repository change."],
          "interface": {
            "inputs": [
              {
                "name": "topic",
                "summary": "The topic and scope for the library.",
                "schemaRef": "#/inputs/0/schema"
              }
            ],
            "outputs": []
          }
        }
      }
    }
  ],
  "tasks": [
    {
      "id": "match-delivery",
      "kind": "match",
      "request": "Implement an approved pagination change in an existing TypeScript repository, run its checks, and prepare the change for review.",
      "expectedWorkflow": "code-delivery"
    },
    {
      "id": "match-library",
      "kind": "match",
      "request": "Research Orlando tourism across attractions, neighborhoods, transport, dining, lodging, and events; collect source media and build a linked knowledge library.",
      "expectedWorkflow": "library-build"
    },
    {
      "id": "no-match-finance",
      "kind": "no-match",
      "request": "Reconcile six months of bank statements, categorize expenses, and prepare a business tax filing package."
    },
    {
      "id": "no-match-operations",
      "kind": "no-match",
      "request": "Compare medical providers, verify insurance coverage, schedule an appointment, and arrange transportation."
    },
    {
      "id": "ambiguous-migration",
      "kind": "ambiguous",
      "request": "Investigate a legacy service and produce a migration brief with evidence and recommended next steps."
    },
    {
      "id": "ambiguous-launch",
      "kind": "ambiguous",
      "request": "Collect competitor material and turn it into a launch-readiness brief for a software product."
    }
  ]
}
