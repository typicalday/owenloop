Build the report.

This body file exercises the `bodyFile` containment check during pack: it is
resolved against the source directory by the single-definition loader and must
stay inside the package.
